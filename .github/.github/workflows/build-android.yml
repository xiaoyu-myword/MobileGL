姓名: 为Android构建MobileGL

在……之上:
workflow_dispatch:

工作:
建立:
连续的: Ubuntu-最新

步骤:
      - 姓名: 签出代码
uses: 操作/结帐@v4
和……一起:
子模块: '递归'

      - 姓名: 设置Android NDK
uses: nttld/setup-ndk@v1
和……一起:
ndk-版本: r26d

      - 姓名: 配置CMake
跑: |
C制作-S. -B构建-G忍者\
-DCMAKE_toolchain_FILE=$ANDROID_NDK_HOME/build/cmake/android。工具链。CMake\
-DANDROID_ABI=arm64-v8a\
-DANDROID_PLATFORM=android-24\
-DBuild_SHARED_LIBS=ON

      - 姓名: 建立
跑: |
CMake--生成内部版本-J$(nproc)

      - 姓名: 上传.so文件
uses: 操作/上载工件@v4
和……一起:
姓名: mobilegl-libs
路径: build/*.so
